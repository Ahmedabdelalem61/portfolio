Ahmed Abdelaleem — React + TypeScript portfolio
Run locally:
1. npm install
2. npm run dev
The project uses Vite + React + TypeScript + TailwindCSS.
Replace src/assets/profile.png with your real photo if needed.
